{
  "name": "grad-na-dlanu",
  "version": "0.1.0",
  "private": true,
  "description": "City directory web app for Žabari (Braničevo, Serbia)",
  "workspaces": [
    "web",
    "server"
  ],
  "scripts": {
    "dev": "npm-run-all --parallel dev:server dev:web",
    "dev:server": "npm --workspace server run dev",
    "dev:web": "npm --workspace web run dev",
    "build": "npm --workspace web run build && npm --workspace server run build",
    "db:up": "docker compose up -d db",
    "db:down": "docker compose down",
    "db:migrate": "npm --workspace server run db:migrate",
    "db:seed": "npm --workspace server run db:seed",
    "db:reset": "docker compose down -v && docker compose up -d db && npm run db:migrate && npm run db:seed"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
